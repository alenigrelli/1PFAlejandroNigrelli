import { TamanoletraDirective } from './tamanoletra.directive';

describe('TamanoletraDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanoletraDirective();
    expect(directive).toBeTruthy();
  });
});
